/**
 * Register React components for use in Slidev slides.
 *
 * Export each component here to make it available via:
 *   <React is="ComponentName" />
 *
 * Example:
 *   import Counter from './Counter'
 *   export default { Counter }
 */

export default {}
